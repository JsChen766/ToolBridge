# ToolBridge

`toolbridge` is a TypeScript CLI that reads `agentTools` from an installed npm package.

v0.1 commands:

- `inspect <package>`
- `validate <package>`
- `run <package> <tool> <json>`
- `mcp <package>` (placeholder)
